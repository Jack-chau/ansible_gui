# auto gen inventory
import yaml

test_data = {

}


class Write_file( self, file_name, path )
    self.file_name = str( input( 'File name: ' ) )
    self.path = str( input( 'path' ) )

    def write_inventory( self, ip, user, sudo_password ) :
        super.__init__( self )
            with open( file_name, 'w' ) as f :
                f.write( '[servers]\n' )
                

    